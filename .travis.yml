env:
  global:
    # The next declaration is the encrypted COVERITY_SCAN_TOKEN, created
    #   via the "travis encrypt" command using the project repo's public key
    - secure: "SDpTydKiCgx4QkL0mFEBz7AkNiJY7rd3c5TQOPcMDa5i4Wp+IE6IsxcVYyUqwax2YAcm003GGv5lTMUbUiILAU6kQ1Vc0iCOXZtneE0YGqQPb9N42G4HuakVJnkSn/CJ/tlWJCZqROx5uAHMkVUDZfzbTOJfmc9X9512At6BEqQ="

language: c
compiler:
  - gcc
  - clang

branches:
  only:
    - sisyphus
    - coverity_scan

#before_install:
#  - echo -n | openssl s_client -connect https://scan.coverity.com:443 | sed -ne '/-BEGIN CERTIFICATE-/,/-END CERTIFICATE-/p' | sudo tee -a /etc/ssl/certs/ca-

install:
  - sudo apt-get install scons

script:
  - if [[ "${COVERITY_SCAN_BRANCH}" == 1 ]];
      then
        echo "Don't build on coverity_scan branch.";
        exit 0;
    fi
  - scons

addons:
  coverity_scan:
    project:
      name: "Etersoft/libnss-role"
      description: "NSS API library and admin tools for roles and privilegies build submitted via Travis CI"
    notification_email: lav@etersoft.ru
    build_command: scons
    branch_pattern: coverity_scan
